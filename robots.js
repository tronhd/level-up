var detroyed;
var weather = "bad";

if(weather === "nice"){
    detroyed = false;
} else if (weather === "okay") {
    destroyed = false;
} else {
    destroyed = true;
}

if (destroyed) {
    console.log("cry in sadness");
} else {
    console.log("jump with glee");
}

var count= 0;
    while(count < 50){
      if(count % 3== 0);
        count = "fizz";{
     }if(count % 5== 0);
        count = "buzz";{
      if(count % 3== 0 && count % 5==0)
          count = "fizzbuzz"
        console.log(count);
        }
        count++}